## 1. Cryptographic foundation

- [x] 1.1 Add a Kotlin Multiplatform cryptography dependency providing AES-256-GCM, HKDF, and ECDH over P-256 across Android and Apple targets, and add a bundled BIP39 wordlist. Verify: both targets compile and a round-trip AES-GCM test passes in `commonTest`. Added `dev.whyoleg.cryptography` 0.6.0 (`cryptography-core` plus `cryptography-provider-optimal`); the wordlist is embedded as `Bip39EnglishWordlist` in `commonMain` rather than as a platform resource so phrase behavior stays testable in `commonTest`. The dependency provides no memory-hard derivation, which Decision 14 resolves by implementing scrypt in `commonMain` under task 6.1.
- [x] 1.2 Implement the record envelope in `commonMain`: version, key identifier, nonce, ciphertext, with serialization to and from the stored document shape. Verify: `commonTest` round-trips a record and asserts the envelope carries all four elements. `EncryptedEnvelopeTest` covers both.
- [x] 1.3 Implement encryption and decryption of `AccountResponse` and `TransactionResponse` payloads against the envelope. Verify: `commonTest` asserts a decrypted record equals the original and that a modified ciphertext or envelope fails decryption without importing anything. `RecordCipherTest` covers round-trip for both record types, tampered ciphertext, tampered nonce, a foreign key, and an unknown envelope version.
- [x] 1.4 Implement fresh random nonce generation per operation, with no content or counter derivation. Verify: `commonTest` encrypts identical input twice and asserts the nonces differ. `NonceUniquenessTest` additionally asserts no repeat across 256 seals of identical input.

## 2. Key hierarchy and platform key storage

- [x] 2.1 Define the key material model as a collection of wrapped copies, each tagged by wrap type, with the data key never represented unwrapped in any stored form. Verify: `commonTest` asserts the serialized model contains no unwrapped key and accepts more than two entries. `KeyMaterialDocument` holds a list of `WrappedDataKey`, each carrying a wrap type, key id, sealed bytes, and an open parameter map so unknown future wrap types decode; `uploadableKeyMaterial()` is the only path to the uploadable subset. `KeyMaterialDocumentTest` (9 tests) covers all of it.
- [x] 2.2 Implement the `expect` device key vault interface for creating a non-extractable P-256 key, performing ECDH, and holding a device secret. Verify: `commonMain` compiles and a fake implementation satisfies the interface for common tests. `DeviceKeyVault` plus `expect fun createDeviceKeyVault()`; placeholder `actual`s in `androidMain` and `iosMain` name tasks 2.3 and 2.4 as owners. `:composeApp:compileKotlinIosSimulatorArm64` and `:composeApp:testAndroidHostTest` both build, and `DeviceKeyVaultTest` (6 tests) drives the software `FakeDeviceKeyVault`.
- [x] 2.3 Implement the Android `actual` over Android Keystore, using StrongBox when the device reports it and hardware-backed Keystore otherwise. Verify: an instrumented test asserts the key is hardware-backed and that private key export fails. Implementation complete in `AndroidDeviceKeyVault`: Keystore-resident P-256 `PURPOSE_AGREE_KEY` on API 31+, and below 31 — where the Keystore cannot agree at all — a software P-256 key sealed under a non-extractable hardware-backed AES-256-GCM Keystore key, with the device secret derived from a Keystore HMAC key on every level and StrongBox requested opportunistically from API 28. `:composeApp:compileAndroidMain` and `:androidApp:processDebugMainManifest` both succeed. Verified in-repo: `composeApp/build.gradle.kts` gained `withDeviceTest {}` with the AndroidJUnit runner and an `androidDeviceTest` dependency block, and `./gradlew :composeApp:connectedAndroidDeviceTest` ran `AndroidDeviceKeyVaultTest` 9/9 green on a physical Motorola edge 60 pro (API 36) — `tests="9" failures="0" errors="0"` — including `deviceKeyMaterialIsHardwareBacked` and `keystoreResidentPrivateKeyCannotBeExported`, which are the two assertions this task's verify clause names. The API-band guarantee is recorded as Decision 15 in `design.md`; on API 24-30 the Keystore cannot perform key agreement at all, so the sealed-software-key fallback is weaker than on API 31+.
- [ ] 2.4 Implement the iOS `actual` over the Keychain and Secure Enclave, restricted to this device and non-extractable. Verify: an `iosSimulatorArm64` test asserts the access-control attributes and export failure, with the Secure Enclave path exercised on a physical device where the simulator cannot represent it. Blocked until the `iosSimulatorArm64` test binary links at all — see the linking risk in `design.md`. Implementation is complete and the test is written but unrun, so this stays unchecked: `KeychainDeviceKeyVault` in `DeviceKeyVault.ios.kt` creates the P-256 key through `SecKeyCreateRandomKey` with `kSecAttrTokenIDSecureEnclave` and an access control of `kSecAttrAccessibleWhenUnlockedThisDeviceOnly` plus `kSecAccessControlPrivateKeyUsage`, falls back to a Keychain-resident key where no Enclave exists (always the simulator, reported by `isSecureEnclaveBacked()`), performs ECDH through `SecKeyCopyKeyExchangeResult` returning the raw secret, and holds the device secret as a `WhenUnlockedThisDeviceOnly` generic password. `KeychainDeviceKeyVaultTest` in `iosTest` covers the encoding, both-direction and cross-implementation ECDH agreement, the access-control attributes, export failure where an Enclave backs the key, and destruction. `:composeApp:compileKotlinIosSimulatorArm64` and `:composeApp:compileKotlinIosArm64` both succeed and the test sources compile, but `:composeApp:linkDebugTestIosSimulatorArm64` still fails with `ld: framework 'FirebaseCore' not found`, so nothing here has been executed.
- [x] 2.5 Implement wrapping and unwrapping of the data key by the device key via ECDH plus HKDF. Verify: `commonTest` covers wrap then unwrap, and asserts unwrapping fails with a different device key. `DeviceKeyWrapper` agrees an ephemeral P-256 key pair against the vault's device key, derives a 256-bit AES key through HKDF-SHA256 under `cofinance/e2ee/data-key-wrap/device/v1`, and seals with a fresh nonce; the ephemeral public key and nonce are stored in `wrap_parameters`. `DeviceKeyWrapperTest` passes 12 of 12 on `:composeApp:testAndroidHostTest`, covering round-trip, a different vault, a destroyed vault, tampered ciphertext, nonce, and ephemeral key, distinct nonces and ciphertext across two wraps, and unwrap after serialization through `KeyMaterialDocument`.

## 3. Recovery phrase

- [x] 3.1 Implement 12-word BIP39 phrase generation with at least 128 bits of entropy, plus checksum validation on entry. Verify: `commonTest` covers generation, valid-phrase acceptance, and rejection of an altered word. `RecoveryPhrase` in `commonMain` generates 128 bits from `CryptographyRandom` and parses entry into a `RecoveryPhraseResult` that separates wrong word count, unknown words, and checksum failure; `RecoveryPhraseTest` passes 13 of 13 on `:composeApp:testAndroidHostTest`, including entropy round-trip, altered-word rejection, and the six published BIP-0039 English vectors.
- [x] 3.2 Implement derivation of the phrase wrapping key and wrapping and unwrapping of the data key with it. Verify: `commonTest` covers wrap then unwrap and asserts a wrong phrase fails to unwrap. `RecoveryPhraseKeyWrapper` derives the wrapping key from the phrase entropy through HKDF-SHA256 with a random per-wrap salt under `cofinance/e2ee/data-key-wrap/recovery-phrase/v1` — a distinct separator from the device wrap's, and no scrypt, since 128 bits of phrase entropy leave nothing to slow down. `RecoveryPhraseKeyWrapperTest` passes 12 of 12 on `:composeApp:testAndroidHostTest`, covering round-trip, a phrase re-parsed from typed text, an unrelated phrase, a phrase one entropy bit away, tampered ciphertext and salt, distinct salts and nonces across two wraps, and unwrap after `uploadableKeyMaterial()` round-trips through serialization.
- [x] 3.3 Build the setup screens: phrase display, the consequence-of-loss statement, and a way to keep the phrase. Verify: setup cannot complete and synchronization cannot begin while the phrase is still on screen, and copying or saving reports honestly what happened. `EncryptionSetupViewModel` and `EncryptionSetupScreen` in `pages/encryption/`. The screen generates the data key and phrase, lays the twelve words out numbered two to a row, and states the consequence of loss above the button that moves past them, alongside a note that the local database is not what this encrypts. There is no re-entry quiz: asking for three words back was satisfied by copying from the screen above it, so it tested nothing while costing every user a typing exercise. In its place the phrase can be copied to the clipboard or written to a file — Downloads through MediaStore on Android, the document picker on iOS — through `RecoveryPhraseExporter` (`data/crypto/`), and a cancelled picker or a refused clipboard is reported as a failure rather than as a saved file. Keeping the phrase is optional; leaving the screen is what completes setup. On completion the order is publish, then store the device wrap locally, then unlock — key material reaches the backend before the session can permit any encrypted write. Setup is reached from the sign-in destination in `MainNavigation`, never from launch, so a local-only user never meets it; a device that finds recovery-phrase key material already published reports `RestoreRequired` instead of generating a second phrase. `EncryptionSetupViewModelTest` passes 9 of 9 on `:composeApp:testAndroidHostTest`: while the phrase is on screen the session stays `SetupIncomplete` with an empty store write log and an encrypted write fails with `DataKeyUnavailableException`, copying and saving neither complete setup nor block it, the exported text is the phrase shown, and after acknowledgement the key material entry precedes every record in the write log while the device wrap stays out of the upload.
- [x] 3.4 Build the restore flow for a device holding no key material: read cloud key material, unwrap with the entered phrase, decrypt and import records, then create a device-bound wrapped copy. Verify: a clean-install restore test imports records, and an invalid phrase imports nothing and permits retry. `RecoveryPhraseRestoreViewModel` and `RecoveryPhraseRestoreScreen` in `pages/encryption/`. The flow is ordered so that it cannot import partially: the phrase is parsed and the stored key material unwrapped before the session is unlocked or a single record is read, both collections are read and decrypted into memory before the first local write, the device-bound wrap is created and stored before the import, and the import itself is two batch upserts. Any failure after the unwrap rolls the session back through `forgetSetup()`. `RecoveryPhraseResult`'s cases are surfaced separately so the screen names the misspelled word and its position, and distinguishes that from a phrase whose words are all real but whose checksum does not hold. `RecoveryPhraseRestoreViewModelTest` passes 8 of 8 on `:composeApp:testAndroidHostTest` against a backend seeded through the production publish and upload paths: a clean-install restore returns the exact records and leaves a device wrap that reopens the same data key locally without being uploaded, an unrelated phrase imports nothing and leaves the session `SetupIncomplete` before succeeding on retry, and a read that fails part-way through the transactions imports no accounts either.

## 4. Encrypted synchronization

- [x] 4.1 Change the Firestore finance data sources — `FirestoreAccountDataSource` and `FirestoreTransactionDataSource`, which replaced the `FirestoreCofinanceDatabase` this change was drafted against — to write encrypted documents for accounts and transactions and to decrypt on read. Verify: a fake-backed test asserts stored documents carry no readable finance fields and that a read returns the original values. The two sources are now `EncryptedAccountDataSource` and `EncryptedTransactionDataSource` over a new `FinanceDocumentStore` port whose only production implementation is `FirestoreFinanceDocumentStore`; the port confines every Firestore type to one class and is what lets a test read back exactly what would be stored. A document holds the plaintext identifier plus `envelope_version`, `key_id`, `nonce`, and `ciphertext`, with every finance field — `updatedAt` included, per Decision 7 — inside the ciphertext. `EncryptedFinanceDataSourceTest` passes 11 of 11 on `:composeApp:testAndroidHostTest`, asserting against the serialized stored document.
- [x] 4.2 Remove the `orderBy` calls on `createdAt` and `date` from the Firestore finance reads and confirm ordering is applied locally. Verify: existing ordering tests over Room observations still pass and no finance-field ordering remains in the Firestore queries. `FinanceDocumentStore.readDocuments` offers no ordering surface at all, so no caller can ask for one, and `grep -rn 'orderBy' composeApp/src` now matches only a comment. Room keeps the ordering it already had — `AccountDao` orders by `createdAt DESC` and `TransactionDao` by `date DESC`, both untouched. There is no pre-existing Room-observation ordering test in the repository to re-run (Room needs a device), so the local-ordering evidence is the unchanged DAO queries plus `EncryptedFinanceDataSourceTest.readsPreserveTheBackendOrderAndApplyNoFinanceOrdering`, which asserts the read path re-sorts nothing.
- [x] 4.3 Store the recovery-phrase wrapped key material beneath the authenticated user's document, written before the first encrypted record. Verify: a test asserts key material exists prior to any encrypted record write and that only the phrase wrap is uploaded. Key material lives at `users/{uid}/key_material/current`, in its own document so profile writes and key material writes cannot overwrite one another. The ordering is enforced by `KeyMaterialGate`: every encrypted write passes `requireKeyMaterial()` first and throws `MissingKeyMaterialException` rather than proceeding, and `publishKeyMaterial` is the only upload path and uploads `KeyMaterialDocument.uploadableKeyMaterial()` rather than filtering wraps itself. The gate remembers which user it confirmed rather than merely that it confirmed, so a second account signing in on the same device cannot inherit the first account's answer. `KeyMaterialUploadTest` passes 8 of 8 on `:composeApp:testAndroidHostTest`, asserting the write log order across key material and records, refusal with nothing stored when material is absent, the account-switch case, and that device and PIN wraps never appear in the stored text.
- [x] 4.4 Gate `FirebaseSyncCoordinator.syncDataAfterSignIn` and `mirrorDataIfSignedIn` — the current names of the `FinanceSyncCoordinator` methods this change was drafted against — on completed encryption setup and an available data key, keeping local mutations durable when the key is unavailable. Verify: tests cover setup-incomplete, locked, and unlocked paths, asserting no plaintext upload in the first two and a mirror after the next unlock. Both paths return rather than throw when `EncryptionSession.dataKeyOrNull()` is null, so a mutation that already committed locally stays committed and the next mirror after an unlock re-uploads the whole snapshot. The gating question is answered by a new `EncryptionSession` (`data/keyring/`), which holds the unwrapped `DataKey` in memory only, reports `SetupIncomplete`, `Locked`, or `Unlocked` through a `StateFlow`, and exposes `lock()` as the seam task 6.5 builds auto-lock on. `EncryptedSyncGatingTest` (6 of 6) drives the real encrypted data sources over a fake store so "no plaintext was uploaded" is asserted against stored text, and `EncryptionSessionTest` (9 of 9) covers the state machine; both pass on `:composeApp:testAndroidHostTest`.
- [x] 4.5 Confirm DRAFT and CYCLE_RESET rows reaching the mirror through `getAllTransactions` are encrypted on the same terms. Verify: a test uploads a draft and asserts its stored document carries no readable finance fields. `EncryptedTransactionDataSource` applies no type filter, so every row the local source hands over is sealed identically. `EncryptedFinanceDataSourceTest.draftTransactionIsEncryptedOnTheSameTerms` and `cycleResetTransactionIsEncryptedOnTheSameTerms` assert that neither the type marker nor any finance value is readable in the stored document and that both round-trip back to the original record.

## 5. Migration of existing plaintext records — removed from scope

Dropped on 2026-08-23 at the user's direction: the backend holds no user data yet, so there is no
plaintext to convert and the flow would ship to convert nothing. Tasks 5.1 through 5.4 had been
implemented and were complete; they are removed rather than left checked, because a checked task
pointing at deleted code reads as delivered capability. The requirement "Existing plaintext records
are migrated in a resumable one-time flow" is out of the `client-side-encryption` delta spec, along
with the unmigrated-record scenario on the envelope requirement. Task 7.4's manual migration pass is
removed with it, and with it that task's dependency on a plaintext-seeded Firebase project.

Task 8.4 records the code removal.

## 6. App lock

- [x] 6.1 Implement scrypt in `commonMain` over the cryptography library's PBKDF2-HMAC-SHA256, per Decision 14, since no Kotlin Multiplatform dependency provides a memory-hard derivation across Android and Apple. Verify: `commonTest` reproduces the RFC 7914 published test vectors. Added `Scrypt` and `ScryptParameters` (`ScryptParameters.PIN` = N=16384, r=8, p=1, 32 bytes); `ScryptTest` reproduces RFC 7914 section 12 vectors 2 and 3 green via `:composeApp:testAndroidHostTest` (10/10). Vector 1 is unreachable because the platform PBKDF2 rejects its empty salt, so `derive` states a non-empty-salt contract instead; vector 4 needs a 1 GiB working set that exceeds the test JVM heap.
- [x] 6.2 Implement PIN-derived key material composed with the device-held secret, using the scrypt derivation from 6.1 before composition, and store the PIN-wrapped copy locally only. Verify: `commonTest` asserts unlock is derivation-based rather than comparison-based, and that the PIN wrap plus correct PIN yields no key under a different device secret. Added `PinKeyWrapper`, wrapping under `HKDF-SHA256(scrypt(pin, salt, ScryptParameters.PIN) || deviceSecret)` with a fresh random salt and nonce per wrap and the `cofinance/e2ee/data-key-wrap/pin/v1` domain separator; both composition inputs are fixed-length so the concatenation is unambiguous, and HKDF's extract is one HMAC over both halves, so neither input can be stripped from the result (XOR and layered wraps were rejected for being invertible or leaving a PIN-only intermediate). Nothing about the PIN is stored: `PinKeyWrapperTest` (9/9 green via `:composeApp:testAndroidHostTest`, whole `data.crypto` package 89/89) asserts the stored wrap holds only a salt, a nonce, and a ciphertext exactly the data key plus a GCM tag, that the sealed bytes move when either the PIN or the device secret changes with salt and nonce held fixed, that a fresh wrapper re-derives the key from PIN plus vault alone, that the correct PIN under a different device vault yields nothing, and that a PIN wrap is excluded from `uploadableKeyMaterial()`.
- [ ] 6.3 Implement the failed-attempt counter in secure storage with delay from the fifth consecutive failure starting at 30 seconds and doubling to a 5-minute cap, destruction of local key material at the tenth consecutive failure, reset on success, and persistence across reinstall. Verify: platform tests cover the delay schedule, destruction at ten, reset, reinstall persistence, and that phrase restoration still works after destruction. Implementation complete and every part of the verification except reinstall persistence has run green; the task stays unchecked because that one cannot be asserted by a test that lives inside a single installation, and because the Android guarantee is narrower than the requirement's wording. What ships: `LockoutPolicy` fixes the schedule (free through the fourth attempt, then `min(30s · 2^(n-5), 5min)` before attempt n, destruction at ten), `FailedAttemptGuard` counts and enforces it against an injected `LockClock`, and `LocalKeyMaterialDestroyer` erases the local wraps, destroys the platform key material, and returns the session to `SetupIncomplete` — with no remote dependency at all, so the uploaded phrase wrap is structurally out of reach. A successful unlock writes a zero record rather than deleting the counter, which makes a later absence detectable; an absent or unopenable record on a device that still holds a PIN wrap is treated as tampering and destroys local key material, closing the "delete the file every four tries" hole. Storage per platform: iOS keeps it in a Keychain generic password, which outlives an uninstall and gives the requirement literally; Android has no such storage, so the record is sealed with AES-256-GCM under a non-extractable Keystore key and the property that actually holds is coupling — clearing app data or uninstalling deletes the counter *and* the Keystore entries the PIN wrap is derived from, so a reinstall gets no further attempts against the same wrapped key because there is no openable wrap left. That distinction is documented on `KeystoreSealedFailedAttemptStore` and is worth a decision in `design.md` before this is called done. Evidence: `:composeApp:testAndroidHostTest` — `LockoutPolicyTest` 5/5, `FailedAttemptGuardTest` 11/11, `AppLockTest` 13/13 (covering the schedule, reset, the tenth failure destroying local material, and the phrase wrap still opening the same data key afterwards); `:composeApp:connectedAndroidDeviceTest` on a physical Motorola edge 60 pro (API 36) — 28/28 green including `AndroidFailedAttemptStoreTest` (8) and `AndroidAppLockDeviceTest` (4), which assert the counter survives a fresh store instance, that its sealing key is Keystore-resident with null `encoded`, that the stored record is non-deterministic ciphertext, that an edited record or one separated from its key reads as unreadable, that the counter and the device key material die together, and that ten wrong PINs over the real Keystore vault destroy local material while the recovery-phrase wrap still yields the identical data key. Remaining and manual only: delete the app, reinstall, and confirm the counter's behaviour on each platform.
- [ ] 6.4 Implement biometric unlock as a shortcut requiring a PIN, with a biometric-invalidating key access policy and PIN fallback. Verify: platform tests cover enabling without a PIN being refused, successful biometric unlock, cancellation falling back to PIN, and a simulated enrollment change leaving the PIN path working. Implementation complete; the task stays unchecked because one named verification — a *successful* biometric unlock on real hardware — cannot be automated at all. No instrumented test can present a finger, and there is no test API that authenticates on the user's behalf, so that path is covered in `commonTest` against a fake box and otherwise needs a manual pass. What ships: `BiometricKeyBox` is the platform seam (seal, open, capability, clear) and `BiometricUnlock` in `commonMain` frames the data key with its identifier and maps every non-success outcome onto one `FellBackToPin` branch, because the response to all of them is identical. Requiring the PIN is structural rather than a flag: `AppLock.enableBiometric` obtains the data key by deriving it from the PIN just entered through `PinKeyWrapper`, so a device with no PIN wrap has nothing to seal and is refused with `PinRequired`, and there is no path that seals an already-unlocked session's key. Android uses a Keystore AES-256-GCM key with `setUserAuthenticationRequired(true)` and `setInvalidatedByBiometricEnrollment(true)`, restricted to `BIOMETRIC_STRONG` and deliberately excluding device credential; iOS uses a Keychain item under `kSecAccessControlBiometryCurrentSet` plus `WhenUnlockedThisDeviceOnly`. Both discard the sealed copy when the platform reports invalidation, so the state after an enrollment change is the state before biometric was ever enabled. Evidence: `:composeApp:testAndroidHostTest` — `AppLockTest` 13/13, covering enabling without a PIN being refused with nothing sealed, enabling being refused on a wrong PIN even while the session is unlocked, a successful biometric unlock yielding the same data key without the PIN, a cancelled prompt falling back to PIN entry without costing a failed attempt, and an invalidated key leaving the PIN path working (six digits rather than a 12-word restore, which is Decision 4's whole point). `:composeApp:connectedAndroidDeviceTest` — `AndroidBiometricKeyBoxTest` 6/6 on a device with a strong fingerprint enrolled, asserting the generated key reports `isUserAuthenticationRequired` and `isInvalidatedByBiometricEnrollment`, that its material never leaves the Keystore, that it cannot encrypt without authentication, and that a sealed copy whose key is gone — the observable state an enrollment change leaves — is discarded and reported as `Invalidated`. Remaining: a manual pass for a real successful prompt, and the iOS box is unrun for the pre-existing `ld: framework 'FirebaseCore' not found` reason. Wiring note for the UI tasks: `BiometricPromptHost.current` must be set to a `FragmentActivity`; the app's `MainActivity` extends `ComponentActivity` today and has to become one, until which every prompt reports `Unavailable` rather than crashing.
- [x] 6.5 Implement in-memory-only data key handling with clearing on background past the auto-lock timeout, and unlock required before finance data is displayed. Verify: tests cover backgrounding past and within the timeout, and assert no unwrapped key is persisted outside process memory while unlocked. `AutoLockController` sits on top of the existing `EncryptionSession` rather than replacing it: it takes the read-only interface, so it can `lock()` and cannot unlock, and locking clears the only copy of the unwrapped key. Two mechanisms cover the two ways background time passes — a scheduled job clears the key *while* backgrounded, and an elapsed-time check on return covers a process that was frozen and never ran the timer — and both read the same injected `LockClock`, so tests advance virtual time instead of sleeping. `AutoLockTimeout` fixes Decision 11's four options with `OneMinute` in effect by default, persisted through `KeyValueAutoLockSettings` over SharedPreferences on Android and `NSUserDefaults` on iOS, storing a stable identifier rather than an ordinal. Unlock-before-display is a property of `EncryptionSession.state`, which `AppLock.state` re-exposes for the unlock screen of task 6.8; nothing here draws UI. Evidence: `:composeApp:testAndroidHostTest` — `AutoLockControllerTest` 7/7 (backgrounded past the timeout clears the key while still in the background, a frozen process locks on return, returning inside the timeout resumes unlocked and the cancelled timer never fires afterwards, `Immediately` locks on leaving the foreground, the default is one minute, and the four options are exactly the specified four) and `AppLockTest.noUnwrappedKeyIsPersistedWhileTheSessionIsUnlocked`, which searches the serialized form of everything the local key material store was ever handed — plus the counter — for the data key in raw and Base64 form while the session is unlocked, and finds neither.
- [x] 6.6 Build the profile security section for setting and changing the PIN, toggling biometric, and choosing the auto-lock timeout from immediately, 1, 5, and 15 minutes with 1 minute in effect by default, requiring the current PIN for changes. Verify: UI tests cover each control, assert the offered timeout options and the default in effect, and assert the current-PIN requirement. `SecuritySettingsViewModel` and `SecuritySettingsSection` (`pages/profile/security/`) render inside `ProfileScreen` and are absent — not disabled — before encryption setup, because a PIN that guards no data key is theatre. One PIN prompt stands in front of every change and carries the intent it authorizes, so the current-PIN requirement is structural rather than per-control: changing the PIN, removing it, turning the biometric shortcut on *or off*, changing the auto-lock timeout, and re-displaying the phrase all go through `AppLock.verifyPin` first, which counts against the same guard as the unlock screen so settings are not an unthrottled PIN oracle. Turning biometric off is included deliberately even though it only tightens the lock: the requirement is written about changing lock settings rather than about weakening them. Setting the *first* PIN is the one action with no current PIN to require, and requires an unlocked session instead. Enabling biometric with no PIN set is refused with an explanation rather than a dead switch, per Decision 4. The threshold from Decision 9 appears nowhere in the state, so no control over it can be rendered. Verified against the real lock rather than a fake of it — `LockFixture` wires the production `AppLock`, `PinKeyWrapper`, and scrypt over the three platform ports only. `SecuritySettingsViewModelTest` 17/17 green via `:composeApp:testAndroidHostTest`: the offered options are exactly `Immediately, OneMinute, FiveMinutes, FifteenMinutes` with `OneMinute` in effect for a user who never chose, a wrong current PIN leaves the timeout, the old PIN, and the biometric state untouched, a correct one applies each, and a changed PIN is proved by the *new* PIN unlocking and the old one no longer doing so rather than by a success flag.
- [x] 6.7 Confirm no unlock screen is presented to a user who has not completed encryption setup. Verify: a local-only launch test asserts direct entry to the main experience. The rule is `launchLockDecision(state)` in `pages/lock/PinRules.kt`: `SetupIncomplete` answers `Main`, and only `Locked` answers `Unlock`. It is a function rather than a branch inside navigation so the rule can be asserted without a navigation host and so a launch path added later cannot invent a different answer. `LaunchLockDecisionTest` 4/4 green via `:composeApp:testAndroidHostTest`, driving a real `AppLock` over a device that has had nothing set up on it: the session reports `SetupIncomplete`, no PIN and no biometric are set, and the decision is `Main`. It also covers the locked and already-unlocked cases and the state after destroyed key material, which routes past the lock to restore rather than sticking on it. The same property is asserted at the routing level by `pages/splash/LaunchRoutingTest` (task 7a.3), whose `aUserWhoNeverSignedInGoesStraightToTheMainExperience` and `aLocalOnlyUserIsNeverShownSetupOrUnlockEvenWithKeyMaterialOnTheDevice` were green in the same run.
- [x] 6.8 Build recovery-phrase re-display in security settings, requiring the current PIN at the moment of the request even when the app is already unlocked. Verify: UI tests assert the phrase is shown only after correct PIN entry, that dismissing or failing the prompt shows nothing, and that an already-unlocked session is still prompted. Re-display needed something that did not exist yet: the recovery-phrase wrap is a copy of the data key sealed *under* the phrase, so it proves a phrase correct but never reveals one, and nothing in the system kept the words. `RecoveryPhraseVault` (`data/crypto/`) closes that: the phrase's 128 bits of entropy sealed under the data key with a fresh salt and nonce per store, written by `EncryptionSetupViewModel.finishSetup` and by a successful restore, over ordinary app-private key-value storage on both platforms. Ordinary storage is correct here — the blob is worth exactly what the data key is worth, and Keychain-style protection would bind to this device the one artifact that exists to outlive it. The PIN is what gates the display: `applyRevealPhrase` calls `AppLock.verifyPin` and reads the vault with the key that derivation produced, never with the session's held key, which is Decision 10 exactly. `SecuritySettingsViewModelTest` (green in the 17/17 above) asserts the phrase appears only after the correct PIN, that a wrong PIN and a dismissed prompt both show nothing, that an already-unlocked session is still prompted and still refused on a wrong PIN, and that a second look is prompted again. `RecoveryPhraseVaultTest` 7/7 covers the seal itself: a different data key opens nothing, an edited blob opens nothing, two stores of one phrase under one key differ, and the stored blob contains none of the words.

## 7. Application wiring

Added during implementation. Groups 1 through 6 build every component this change needs, but three seams between them were owned by no task, and without them the feature is built rather than working: the device wrap does not outlive the process, migration is never invoked, and a relaunching signed-in user reaches finance data without unlocking.

- [x] 7a.1 Provide a single durable local key material store with platform-backed implementations, replacing the in-memory placeholder. Two ports were defined independently — `data/lock/LocalKeyMaterialStore.kt` and `pages/encryption/LocalKeyMaterialStore.kt` — so reconcile them to one, sited in a data package rather than a UI one. Verify: an Android device test asserts a stored device wrap is readable from a fresh instance after process restart, and a `commonTest` asserts the store never exposes an unwrapped key. Implementation complete. `pages/encryption/LocalKeyMaterialStore.kt` is deleted along with its `InMemoryLocalKeyMaterialStore`, and `data/lock/LocalKeyMaterialStore` — the port that already carried `erase()`, which destruction after ten failed attempts needs — is the only one left; setup, restore, the lock, and the destroyer all hold it, and `FakeLocalKeyMaterialStore` is the single fake. The durable implementation is split so that the part worth testing is testable: `StoredLocalKeyMaterialStore` in `commonMain` serializes `KeyMaterialDocument` to bytes, and the `expect fun createKeyMaterialStorage()` behind it only moves bytes. Nothing is encrypted a second time, and that is a decision rather than an omission — every element of `wrappedKeys` is already sealed against platform key material that cannot leave the device, so a second layer would protect nothing and would add a key whose loss strands the wraps behind it. Android: a file in the lock's app-private directory, written through a temporary file and renamed, so a process death mid-write leaves the previous document rather than a truncated one. This is the asymmetry with the failed-attempt counter, which *is* Keystore-sealed because a bare integer has no protection of its own. iOS: a Keychain generic password under `kSecAttrAccessibleWhenUnlockedThisDeviceOnly` — `ThisDeviceOnly` because a synchronizing item would carry the device wrap to a second device and quietly make the recovery phrase not the only route there, and `WhenUnlocked` rather than the counter's `AfterFirstUnlock` because this document is only ever read in the foreground. The store cannot hold an unwrapped key structurally: `KeyMaterialDocument` has no field one could occupy. Evidence so far: `:composeApp:testAndroidHostTest` — `StoredLocalKeyMaterialStoreTest` 5/5, asserting a fresh store instance reads back what an earlier one wrote, that erase removes the bytes rather than blanking the document, that unparsable bytes read as absent rather than throwing (a launch that cannot decode its key material must reach restore, not fail to start), and that neither the raw nor the Base64 form of a real data key appears in the stored bytes; `:composeApp:compileKotlinIosSimulatorArm64` green for the Keychain half. `:composeApp:connectedAndroidDeviceTest` — `AndroidLocalKeyMaterialStoreTest` 4/4, asserting that a store built over a fresh `createKeyMaterialStorage()` reads back the document an earlier store wrote with nothing but the filesystem between them, that the restored device wrap still opens the identical data key through the real `DeviceKeyWrapper` and the real platform vault, that the document is a file under `filesDir` carrying neither the raw nor the Base64 form of the key, and that erase removes the file rather than emptying it. Run on an API 37 emulator rather than the physical Motorola, which was not attached; the properties asserted here are filesystem and serialization properties that do not depend on hardware-backed key storage, so the substitution does not weaken them.
- [x] 7a.2 Register the migration components in dependency injection and invoke `PlaintextMigration.run()` on the blocking launch path for a signed-in user holding plaintext records. Verify: a test asserts migration runs before finance data is reachable, and that a user with no plaintext records is not blocked. `securityModule` registers `FirestorePlaintextFinanceDocumentStore`, `PlaintextRecordMigrator`, and `PlaintextMigration`, and supplies the `EncryptionSetup` seam as an assertion rather than an action — it asks the session for the data key, so a run that reaches conversion without completed setup fails before the first ciphertext instead of sealing records under key material that was never published. Ordering is the point: the launch sequence runs setup, then unlock, then migration, then synchronization, so migration always has a data key and finance data is never synchronized ahead of it. Blocking costs a scan and nothing more for a clean account — `PlaintextMigration.run()` returns `Complete(0, 0)` from the scan without touching `EncryptionSetup` or writing anything. Evidence: `:composeApp:testAndroidHostTest` — `LaunchRoutingTest` 11/11, of which `migrationRunsBeforeFinanceDataIsReachable` asserts the stored operations are exactly `write` then `delete` for the plaintext record before the route settles on main, `aUserWithNoPlaintextRecordsIsNotBlocked` asserts an account with nothing to convert reaches main with an empty operation log, `aLocalOnlyUserIsNotBlockedByMigration` asserts a signed-out user's seeded plaintext is never touched, and `aFailedMigrationHoldsTheLaunchAndOffersARetryThatFinishesIt` asserts an interrupted run holds the launch with no route at all and that the retry completes it.
- [x] 7a.3 Gate the splash and launch routing on encryption state so a signed-in user with a configured PIN unlocks before finance data is displayed, and a local-only user is unaffected. Verify: tests cover local-only launch reaching the main experience directly, a signed-in locked launch reaching unlock, and a signed-in unlocked launch reaching main. `SplashViewModel` is now the whole launch sequence and the only place the decision is made: not signed in yields main with no setup, no unlock, and no migration; otherwise the durable key material is adopted into the process-local session, an absent document yields setup, and `launchLockDecision` — task 6.7's rule, called rather than duplicated — yields unlock for a session holding key material it cannot open. The PIN check precedes the device wrap deliberately: the device wrap would open the session without asking, so `AppLock.isPinSet()` is what stops it. A device wrap that no longer opens with no PIN behind it returns the session to `SetupIncomplete` rather than sitting at a locked state nothing can satisfy; setup then finds the account's published key material and hands over to restore. Every edge that produces an unlocked session — sign-in, setup completion, restore completion, unlock — re-enters the sequence instead of jumping to main, so migration cannot be skipped by arriving from the side. Auto-lock is wired in `App.kt` through `LifecycleEventEffect` on `ON_STOP`/`ON_START` (not pause/resume, which a permission dialog also fires), and `MainNavigation` returns the app to the launch sequence when the session goes `Locked` anywhere outside it, so the timeout firing actually removes finance data from view. Evidence: `:composeApp:testAndroidHostTest` — `LaunchRoutingTest` 11/11 over the real `AppLock` and the real `PlaintextMigration`, plus an observed launch on an API 37 emulator where a never-signed-in install reached the main Activity screen with no setup and no unlock in between. The tests cover local-only launch reaching main (including with stale key material on the device, which must still not prompt), a signed-in device with a PIN reaching unlock with the session still `Locked` and nothing synchronized, a signed-in device without a PIN opening from the device wrap and reaching main with the same key, an already-unlocked session reaching main, and a signed-in device with no key material reaching setup without synchronizing first.
- [x] 7a.4 Make the Android host activity a `FragmentActivity` and supply it to `BiometricPromptHost`, which `androidx.biometric` requires. Until this lands biometric prompts report `Unavailable` rather than working. Verify: a device test drives a biometric prompt through the real host, or, where a prompt cannot be automated, asserts the host resolves to a `FragmentActivity`. Implementation complete: `MainActivity` extends `AppCompatActivity`, sets `BiometricPromptHost.current` in `onCreate`, and clears it in `onDestroy` only when it is still its own reference, so a recreated activity is not unregistered by its predecessor. `AppCompatActivity` rather than a bare `FragmentActivity` because `minSdk` is 24: below API 28 `androidx.biometric` draws its own fingerprint dialog out of AppCompat resources, which is why the library declares a runtime dependency on AppCompat, so a bare `FragmentActivity` under the previous `Theme.Material.Light.NoActionBar` would have worked on a modern device and failed on the older half of the install base. The window theme moves to `Theme.AppCompat.Light.NoActionBar` accordingly; the app's colors come from Compose either way. `AndroidBiometricPromptHostTest` is written and asserts the seam directly — that with no host every capability query returns `Unavailable` regardless of hardware, and that with a real `FragmentActivity` installed the same query reaches `BiometricManager` — against a bare `FragmentActivity` declared in a new `androidDeviceTest` manifest, because the app's own activity lives in `androidApp` and is not on that module's classpath. Evidence: `:composeApp:connectedAndroidDeviceTest` — `AndroidBiometricPromptHostTest` 3/3, asserting the pre-wiring state (no host, `Unavailable` regardless of hardware), that a real host makes the query answer about the device instead, and that the host reference is a `FragmentActivity`. `:androidApp:installDebug` then a launch through `adb`: Koin started 89 definitions with no unresolved dependency, `MainActivity` became `topResumedActivity` with no `FATAL`/`AndroidRuntime` entry in logcat, the app rendered the main Activity screen, and a home-then-resume round trip through the new `AutoLockController` lifecycle hook left the same process alive. Run on an API 37 emulator; the physical device was not attached.

## 7. Verification and specification closure

- [ ] 7.1 Run the full `commonTest` suite plus the Android and iOS platform checks from groups 2 and 6. Verify: all suites pass on both targets. **Android is now fully green; iOS is not, so this stays unchecked.**

Shared suites: `:core:testAndroidHostTest` 146/146 and `:composeApp:testAndroidHostTest` 139/139, zero failures. `:androidApp:assembleDebug` and `:composeApp:linkReleaseFrameworkIosArm64` both succeed.

Android platform checks, on the physical motorola edge 60 pro (API 36) rather than an emulator: `:core:connectedAndroidDeviceTest` runs 41 and fails 3. The 38 passing cover every group 2 and group 6 check, including `AndroidDeviceKeyVaultTest.deviceKeyMaterialIsHardwareBacked` — the one an emulator cannot satisfy by construction, and the gap this task previously recorded. The 3 failures are `MlKitSampleReceiptTest`, all reporting `Missing test asset: sample_receipt.jpg`; they belong to task 1.3 of `on-device-receipt-scanning`, not to any group here, and fail rather than skip on purpose so an absent receipt cannot read as verified.

iOS platform checks: `:core:iosSimulatorArm64Test` links and runs 168, passing 152. The 16 failures are the whole of `KeychainDeviceKeyVaultTest` and `KeychainFailedAttemptStoreTest`, each dying at its first Keychain call with `errSecNotAvailable`, because a Kotlin/Native test binary has no app bundle and therefore no keychain. Closing this needs the same XCTest-with-host-application harness that task 2.4 needs; until then "all suites pass on both targets" is not true and this task is not done.

Two cheaper routes were tried on 2026-08-24 and neither works, recorded so they are not retried. Embedding an entitlements blob into the test binary with `-sectcreate __TEXT __entitlements` does land the section — `otool -l` shows 537 bytes of it — but the keychain stays unavailable, because securityd will not honour entitlements the code signature does not cover. Ad-hoc signing the linked `test.kexe` with those same entitlements does produce a correctly-entitled binary, and then `simctl spawn` refuses to launch it at all (`LaunchdSimError` 153). Both changes were reverted; the build carries no leftover machinery from either.

- [ ] 7.2 Perform a manual end-to-end pass: sign in, complete setup, record a transaction, then inspect the stored document directly in the backend console. Verify: no finance value is readable.
- [ ] 7.3 Perform a manual restore pass on a clean install using only the recovery phrase. Verify: synchronized data returns and a device-bound wrapped copy is created.
- [x] 7.5 Resolve the open questions recorded in `design.md` — failed-attempt threshold, phrase re-display policy, optional export file, auto-lock default and options, DRAFT and CYCLE_RESET upload scope — and record the decisions in the design document before the affected tasks are marked complete. Verify: no open question remains that governs a shipped behavior. Recorded as Decisions 9 through 13 in `design.md`; the Open Questions section is empty.
- [x] 7.6 Run `openspec validate "end-to-end-encrypted-sync" --strict`. Verify: validation passes. Reports `Change 'end-to-end-encrypted-sync' is valid`.
- [ ] 7.7 Synchronize the delta specs for `client-side-encryption`, `app-lock`, `firebase-data-backend`, and `offline-data-sync` into `openspec/specs/`, then validate the main specs. Verify: main specs validate and reflect the encrypted synchronization behavior. Deliberately deferred rather than blocked. Synchronizing these deltas into `openspec/specs/` asserts that the capability is delivered, and three of its load-bearing claims have not been observed yet: that a stored document carries no readable finance value in the real backend (7.2), that a clean-install restore returns the data from the phrase alone (7.3), and that migration converts a project seeded with real plaintext (7.4). Each needs a device and a live Firebase project. Sync after those pass, not before.

## 8. Firebase-free `:core` module (Decision 17)

- [x] 8.1 Create the `:core` Gradle module with the Android, `iosArm64`, and `iosSimulatorArm64` targets and no Firebase dependency, and register it in `settings.gradle.kts`. Verify: `./gradlew projects` lists it and `:core:compileKotlinIosSimulatorArm64` succeeds. Both hold. The module carries the Android, `iosArm64`, and `iosSimulatorArm64` targets and depends only on kotlinx, `dev.whyoleg.cryptography`, ML Kit, and `androidx.biometric` — no Firebase on any path. It packages no framework of its own; `composeApp` keeps that job.
- [x] 8.2 Move `data/crypto`, `data/lock`, `data/keyring`, the non-parser `data/ocr` files, and `AccountResponse`/`TransactionResponse` into `:core` with their tests, keeping every package name unchanged, and give `composeApp` an `api` dependency on `:core`. Verify: `:composeApp:testAndroidHostTest` still reports the same test count with no failures and no import in `composeApp` was edited. Both hold: 306 tests before the move, 306 after — now 146 in `:core` and 160 in `:composeApp` — with 0 failures on either side, and not one `import` statement changed, because every package name was preserved and `composeApp` takes `api(projects.core)`.

That host-test check was not sufficient on its own, and running the device suite afterwards proved it: the move had broken all 35 Android instrumented tests. `DeviceKeyVaultStorage` reaches app-private storage through an application `Context` that `DeviceKeyVaultInitializer`, a `ContentProvider`, captures at process start — and that provider was declared in `composeApp`'s `AndroidManifest.xml`, which did not travel with the class. Every test failed with "Device key storage is unavailable because the application context was never captured", and the whole Android lock reaches its storage the same way. The declaration now lives in `core/src/androidMain/AndroidManifest.xml`, next to the class it names; a library manifest merges into every consumer, so the app still gets it and this module's instrumented APK gets it too. Verified on hardware below. One thing the plan did not anticipate: a Kotlin test source set is not visible to another module's tests, so the fakes in `LockFakes.kt` and `FakeDeviceKeyVault.kt` — which `composeApp`'s own view-model suites drive against — could not stay in `:core`'s `commonTest`. They live in a third module, `:core-testing`, whose `commonMain` both test source sets depend on; putting them in `:core`'s `commonMain` would have shipped test doubles in the product.
- [x] 8.3 Confirm the iOS test binary links and run the iOS tests. Verify: `:core:linkDebugTestIosSimulatorArm64` succeeds and `:core:iosSimulatorArm64Test` runs, which is what tasks 2.4 and 7.1 here, and 1.4 of `on-device-receipt-scanning`, are waiting on. **The link succeeds and the tests run** — the first iOS test execution this repository has ever had. 168 tests, 152 passing.

Running them found one real defect, in a test rather than in shipping code. `VisionCoordinateConversionTest.keeps top above bottom for every box` asserted a centre of `0.4` for a Vision box spanning Y 0.3..0.5. Vision measures Y upward from the bottom, so that box's centre is 0.4 above the bottom edge and therefore **0.6** below the top one: the centre flips along with the edges, and the expectation had flipped the edges but not the centre. `visionBoxToTopLeftRect` was correct; the assertion was not. Corrected, and `VisionCoordinateConversionTest` is now 6/6. That is task 1.4 of `on-device-receipt-scanning` verified as written.

The 16 remaining failures are all in the two suites that touch the Keychain — `KeychainDeviceKeyVaultTest` (9) and `KeychainFailedAttemptStoreTest` (7) — and every one of them fails at its first Keychain call with `OSStatus -25291`, which `SecBase.h` defines as `errSecNotAvailable`, "No keychain is available." A Kotlin/Native test binary runs in the simulator as a bare executable with no app bundle and no application-identifier entitlement, so Keychain Services have no keychain to offer it. This is a property of the harness, not of `KeychainDeviceKeyVault` or `KeychainFailedAttemptStore`: no assertion about their behaviour was reached, let alone failed. Running these two suites needs an XCTest bundle with a host application, which is a separate piece of work from this one; task 2.4 therefore stays unchecked, but its blocker is now this narrow one rather than "no iOS test can run at all".

- [x] 8.4 Remove the plaintext-migration flow: delete `data/migration`'s migration files and their tests, relocate `EncryptionSetup` (a setup seam `MainNavigation` also uses, not a migration type), drop the `Migrating` and `MigrationFailed` launch phases and their strings, and remove the DI bindings. Verify: no source reference to `PlaintextMigration`, `MigrationState`, or `PlaintextRecordMigrator` remains, `:composeApp:testAndroidHostTest` passes, and `:androidApp:assembleDebug` succeeds. All three hold. Deleted: `data/migration/` in both `commonMain` and `commonTest` — `PlaintextMigration`, `PlaintextRecordMigrator`, `PlaintextFinanceDocumentStore`, `FirestorePlaintextFinanceDocumentStore`, `LegacyFinanceCollection`, and their tests. `EncryptionSetup` went with them rather than being relocated: it turned out to be reached only by migration and by the DI binding that fed migration, and the `EncryptionSetup` names in `MainNavigation`, `Destinations`, and `LaunchRoute` are unrelated types that merely share the word.

Also removed: the `Migrating` and `MigrationFailed` launch phases, `SplashUiEvent` in full — with migration gone the launch screen has no events at all, so `onEvent` and the screen's `MessageContent` retry went too — the three migration strings in both locales, and the DI bindings.

Test count moves from 306 to 285, and the 21 are exactly accounted for: `PlaintextMigrationTest` had 18, and `LaunchRoutingTest` drops from 11 to 8 as its three migration cases go. Its fourth migration-flavoured case, a signed-out user not being blocked by migration, was kept and retargeted onto the claim that still means something — that a signed-out launch never reaches the cloud. Evidence: `:core:testAndroidHostTest` 146/146 and `:composeApp:testAndroidHostTest` 139/139 green, `:androidApp:assembleDebug` and `:composeApp:linkReleaseFrameworkIosArm64` both succeed, and `:core:iosSimulatorArm64Test` is unchanged at 152/168.
