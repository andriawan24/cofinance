# Project-specific R8/ProGuard rules.
#
# Keep this file in the repo because the release build references it during
# APK assembly in local builds and GitHub Actions. Add concrete keep rules here
# if release shrinking ever needs them.
